#tests
  -@protocol.challenges.each do |ch|
    %p
      %b Dane do walidacji
      = ch.data
      %br
      %b Oczekiwany rezultat
      - if ch.result
        prawda
      - else
        fałsz
      %br
      %b Uzyskany resultat
      %br
      = link_to 'Edytuj test', edit_protocol_challenge_path(@protocol, ch) 
  = link_to 'Nowy test', new_protocol_challenge_path(@protocol)
  
